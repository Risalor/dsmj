Identifier = letter, { letter | digit | "_" } ;
String = '"', { character }, '"' ;
number = digit, { digit } ;
boolean = "true" | "false" ;
letter = "a" | "b" | ... | "z" | "A" | "B" | ... | "Z" ;
digit = "0" | "1" | ... | "9" ;

"Ryce" "<=" "{" 
    Menu,
    Api,
    Images,
    Footer?
"}"

Menu:
    Identifier ":" "<Menu>" "<=" "{"
        "title" "<=" "{"
            { Option "," }+
        "}" ","
        "options" "<=" "{"
            { Option "," }+
        "}" ","
        ("set_check" ":" String ",")?
        ("options_is_set" "<=" "{"
            { Option "," }+
        "}" ",")?
        ("options_not_set" "<=" "{"
            { Option "," }+
        "}" ",")?
        "type" ":" Type
"}"

Type:
    "topbar" | "sidebar"